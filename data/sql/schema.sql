CREATE TABLE article (id BIGINT AUTO_INCREMENT, title VARCHAR(100) DEFAULT '' NOT NULL, text LONGTEXT DEFAULT '' NOT NULL, image_url LONGTEXT DEFAULT '' NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, slug VARCHAR(255), UNIQUE INDEX article_sluggable_idx (slug), PRIMARY KEY(id)) ENGINE = INNODB;
